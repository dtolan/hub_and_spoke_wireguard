# WireGuard Hub-and-Spoke Dashboard Configuration
# Copy this file to .env and fill in your values

# API endpoint for WireGuard management (if applicable)
# VITE_API_ENDPOINT=http://localhost:3000/api

# Default hub endpoint (external IP:port)
# VITE_DEFAULT_HUB_ENDPOINT=your-server-ip:51820

# Default network CIDR for hub-and-spoke topology
# VITE_DEFAULT_NETWORK_CIDR=10.0.0.0/24

# Enable debug mode
# VITE_DEBUG=false
